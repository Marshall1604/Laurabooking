import type{MetadataRoute}from'next';import{services,destinations}from'@/lib/content';export default function sitemap():MetadataRoute.Sitemap{const base='https://aurelis.example';return['','/services','/destinations','/partner-with-us','/booking',...services.map(s=>`/services/${s.slug}`),...destinations.map(d=>`/destinations/${d.slug}`)].map(url=>({url:base+url,lastModified:new Date()}))}
